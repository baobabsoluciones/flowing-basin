"""
heuristic.py
This script analyzes the performance of the heuristic and compares it with rl-greedy
"""

from flowing_basin.solvers import Baseline

# Baseline(general_config='G0', solver='rl-greedy').solve(instance_names=['Percentile20'])
# Baseline(general_config='G0', solver='rl-random').solve(instance_names=['Percentile20'])
# Baseline(general_config='G0', solver='rl-A1G0O1R1T1').solve(instance_names=['Percentile20'])
